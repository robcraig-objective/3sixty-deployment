# MongoDB credentials are managed externally — see {release}-admin-secret and
# {release}-scim-secret created by kubectl-create-secrets.ps1 or aks-create-secrets.ps1.
# This file is retained as a placeholder only; no Secret object is created here.
