# ====================================================================
# MONGO SUBCHART VERSIONING
# ====================================================================
# This subchart deploys MongoDB for 3Sixty Admin and Discovery services.
#
# version: Update when modifying this subchart's templates or values
#   - Template changes, resource updates, config modifications
#   - Must update parent Chart.yaml dependency version to match
#
# appVersion: Update when changing MongoDB image tag
#   - Tracks MongoDB version (e.g., 8.0.11)
#   - See values.yaml image.tag field
# ====================================================================

apiVersion: v2
name: mongo
description: MongoDB database subchart for 3Sixty Admin and Discovery services
type: application
version: 1.0.0  # Subchart version - update when modifying templates/values
appVersion: "8.0.11"  # MongoDB version - update when changing image tag
