CLIENT_ID=<client-id>
TENANT_ID=<tenat-id>
CLIENT_SECRET=<client-secret>
SCOPES=openid,profile,email,offline_access,api://<application-client-id>/user_impersonation

VAULT_ENABLED=false
VAULT_ADDR=http://vault:8200
VAULT_TOKEN_FILE=/vault/.vault-token

TZ=Australia/Sydney
