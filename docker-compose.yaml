version: "3.8"

services:
  nginx:
    image: nginx:1.28.0-alpine
    container_name: nginx-proxy
    ports:
      - "80:80"
      - "443:443"  # expose HTTPS
    depends_on:
      - threesixty-admin
      - threesixty-discovery
      - scim-server
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/certs/tls.crt:/etc/nginx/certs/tls.crt:ro
      - ./nginx/certs/tls.key:/etc/nginx/certs/tls.key:ro
    networks:
      - threesixty

  threesixty-admin:
    image: ghcr.io/objective-corporation/3sixty/threesixty:5.0.0-RC2
    container_name: threesixty-admin
    env_file:
      - ./.env.admin
    environment:
      SSL_ENABLED: false
    depends_on:
      - mongo
    networks:
      - threesixty
    #volumes:
    #  - ../my-data:/usr/local/tomcat/localMount:rw

  threesixty-discovery:
    image: ghcr.io/objective-corporation/3sixty/discovery:5.0.0-rc2
    container_name: threesixty-discovery
    env_file:
      - ./.env.discovery
    environment:
      SSL_ENABLED: false
    depends_on:
      - mongo
      - threesixty-admin
    networks:
      - threesixty
    #volumes:
    #- ./vault-agent/.vault-token:/vault/.vault-token:ro

  mongo:
    image: ghcr.io/objective-corporation/3sixty/mongo:5.0.0-rc2
    container_name: mongo
    ports:
      - "27017:27017"
    env_file:
      - ./.env.mongo
    restart: always
    volumes:
      - mongo_data:/data/db
    networks:
      - threesixty

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:7.17.0
    container_name: elasticsearch
    environment:
      - discovery.type=single-node
      - ES_JAVA_OPTS=-Xms512m -Xmx512m
    ports:
      - "9200:9200"
      - "9300:9300"
    volumes:
      - es_data:/usr/share/elasticsearch/data
    networks:
      - threesixty

  # Declarative Agent
  oi-rag:
    image: ghcr.io/objective-corporation/3sixty/oi-rag:3sixty
    container_name: oi-rag
    env_file:
      - ./.env.oirag
    volumes:
      - ./logs:/oi-rag-logs
    depends_on:
      - opensearch
      - ollama
    networks:
      - threesixty

  ollama:
    image: ollama/ollama:0.7.0
    container_name: ollama
    volumes:
      - ollama_data:/root/.ollama
    entrypoint: [ "bash", "-c" ]
    command: >
      "ollama serve & sleep 5 && ollama pull mxbai-embed-large && wait"
    networks:
      - threesixty

  opensearch:
    image: opensearchproject/opensearch:2.4.1
    container_name: opensearch
    environment:
      http.host: 0.0.0.0
      network.host: 0.0.0.0
      transport.host: 127.0.0.1
      discovery.type: single-node
      compatibility.override_main_response_version: true
      bootstrap.memory_lock: true # Disable JVM heap memory swapping
      OPENSEARCH_JAVA_OPTS: "-Xms2g -Xmx2g"
    volumes:
      - opensearch_data:/usr/share/opensearch/data:rw
    networks:
      - threesixty

  opensearch-dashboard:
    image: opensearchproject/opensearch-dashboards:2.4.1
    container_name: opensearch-dashboard
    ports:
      - "15601:5601"
    environment:
      OPENSEARCH_HOSTS: '["https://opensearch:9200"]'
    depends_on:
      - opensearch
    networks:
      - threesixty

  # SCIM user provisioning
  rabbitmq:
    image: rabbitmq:4.1-management
    container_name: rabbitmq
    env_file:
      - ./.env.rabbitmq
    ports:
      #   - "5672:5672"
      - "15672:15672"
    networks:
      - threesixty
  
  scim-server:
    image: ghcr.io/objective-corporation/3sixty/scim-server:1.0.5
    container_name: scim-server
    env_file:
      - ./.env.scim
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - threesixty

volumes:
  opensearch_data:
  mongo_data:
    name: docker_mongo_data
  es_data:
  ollama_data:

networks:
  threesixty:
    name: threesixty
    driver: bridge
